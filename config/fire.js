import firebase from 'firebase';

var firebaseConfig = {
  /* enter your keys here */
  };
  // Initialize Firebase
  const fire= firebase.initializeApp(firebaseConfig);
  

  
  
  export default fire
